---
description: 
globs: 
alwaysApply: true
---
你是精通 TypeScript、Node.js、Vite、Vue.js、Vue Router、Pinia、VueUse、Element Plus的技术专家,深刻理解这些技术的最佳实践和性能优化方法。

# 1.代码规范

## 1.1 命名

### 1.1.1 变量命名规范

TypeScript 中的变量命名规范：

### 1.1.2 基本变量命名

```typescript
// 使用 camelCase
let firstName: string;
let userAge: number;
let isActive: boolean;

// 常量使用 UPPER_SNAKE_CASE
const MAX_COUNT: number = 100;
const API_BASE_URL: string = "https://api.example.com";
```

### 1.1.3 接口命名

```typescript
// 使用 PascalCase,可以加前缀 I
interface IUserProfile {
  id: number;
  name: string;
}
```

### 1.1.4 类型别名

```typescript
// 使用 PascalCase
type UserRole = "admin" | "user" | "guest";
type ValidationResult = {
  isValid: boolean;
  errors: string[];
};
```

### 1.1.5 枚举

```typescript
// 使用 PascalCase
enum HttpStatus {
  OK = 200,
  NotFound = 404,
  InternalError = 500,
}

// 常量枚举也是 PascalCase
const enum Direction {
  Up = "UP",
  Down = "DOWN",
}
```

### 1.1.6 类命名

```typescript
// 使用 PascalCase
class UserService {
  private readonly apiClient: ApiClient;
}
```

### 1.1.7 私有成员

```typescript
class Example {
  // 类全局的直接使用 private 修饰符(更推荐)
  private count: number;

  // 类局部的私有属性可以使用下划线前缀
  private _count: number;
  aaa() {}
}
```

### 1.1.8 函数命名

```typescript
// 动词开头,使用 camelCase
function getUserData(): User;
function validateInput(value: string): boolean;
function handleClick(event: MouseEvent): void;

// 布尔返回值的函数通常用 is/has/can 等开头
function isValidEmail(email: string): boolean;
function hasPermission(user: User, action: string): boolean;
```

### 1.1.9 泛型参数

```typescript
// 通常使用单个大写字母或有意义的 PascalCase
interface List<T> {
  items: T[];
}

// 多个泛型参数
interface Map<K, V> {
  get(key: K): V;
}

// 更具描述性的泛型名称
interface Repository<TEntity> {
  findById(id: string): Promise<TEntity>;
}
```

### 1.1.10 文件命名

```typescript
// 组件文件使用 PascalCase
UserProfile.vue;
LoginForm.vue;

// 其他文件使用 camelCase
userErvice.ts;
apiClient.ts;
```

### 1.1.11 命名约定

1. **避免**：

   - 单字母命名(除了循环中的 i, j, k)
   - 含糊的命名(data, info, thing)
   - 过长的命名
   - 拼写错误

2. **推荐**：

   - 使用完整的单词
   - 使用有意义的命名
   - 保持一致的命名风格

3. **特殊情况**：

```typescript
// 回调函数参数
interface CallbackOptions {
  onSuccess?: (data: any) => void;
  onError?: (err: Error) => void;
}

// 事件处理函数
const handleClick = (e: MouseEvent) => void;
const handleChange = (value: string) => void;
```

遵循这些命名规范可以：

- 提高代码可读性
- 保持团队代码风格统一
- 减少命名冲突
- 更容易维护和重构

## 1.2. ts, vue语言规范

### 1.2.1 ts规范

1. 所有代码使用 TypeScript
2. 优先选择可扩展和可合并的接口(interface)而非类型别名(type)
3. 尽量避免用any
4. 使用enum/object来代替硬编码
5. 完美地使用类型虽然会带来比较大的心智负担, 但是带来的收益也很可观
   - 完整的类型提示, 避免遗忘, 类型本身也是注释
   - 完整的类型可以让编译器提前识别出潜在的问题, 减少bug
   - 随着熟练度上升逐渐, 心智负担也会越来越轻
6. try-catch
7. 优先使用支持async的异步函数, 避免使用回调形式的异步函数
8. 异步函数需要await, 避免影响执行顺序

### 1.2.2 vue规范

1. 使用带有 TypeScript 接口的函数式组件
2. 尽量使用严格的组件实例的类型
3. 在适当场景应用 VueUse 函数增强响应性和性能
4. 异步组件使用 Suspense 包装并设置回退 UI
5. 非关键组件采用动态加载
6. 图片优化：使用 WebP 格式、包含尺寸信息、实现懒加载
7. 在 Vite 构建过程中实施优化的代码分割策略（如代码拆分）以生成更小的包体积

### 1.2.3 其它编程规范

1. 避免硬编码
2. 只使用英文标点
3. 错误处理与日志记录

# 2. 项目规范

## 2.1 技术栈

1. 使用 Vite 实现现代化快速开发（支持热模块替换）
2. 应用 Vue Router 实现 SPA 导航和动态路由
3. 采用 Pinia 实现模块化状态管理
4. stereov
5. electron
6. chromium
7. element-plus
8. icon-font
9. vue3
10. ts
11. git
12. i18n
13. axios
14. lodash
15. plotly
16. yarn
17. less
18. prettier+eslint
19. vue-use

## 2.2 文件目录结构

按 src/components 组织组件,路由使用懒加载

<!-- service 公共服务
view 视图
components 子组件 -->

## 2.3 基础类

### 2.3.1 角色

a. **Manager** 侧重作为管理角色
b. **Controller** 侧重作为用户操作支持的角色
c. **Service** 侧重作为服务支持的角色
d. **Helper** 侧重作为工具人, 助手的角色
e. 其它

### 2.3.2 基础类命名方式

1. **XxxManager** (业务)管理类
    a. 比如FeatureManager, 用于管理业务逻辑
2. **XxxController** 控制器类
    a. 与用户交互相关的逻辑封装, 比如缩放,旋转, 或者测量等
    b. 需要做好通过方法调用来模拟用户交互的封装, 以简化单元测试
    c. 需要做单元测试
3. **XxxService** 公共服务类
    a. 比如ConfigService, LanguageService等需要全局访问的服务
    b. 需要做单元测试
4. **XxxHelper** 辅助类
    a. 比如FileHelperEventHelper这种通用性较强的独立封装类
    b. 需要做单元测试
5. 其余类
    a. 一些比较底层的类, 比如PyramidTask这种不好规定其角色,  通用性较差, 一般只在特定业务场景下用到, 自行命名, 一般来说可以看作与辅助类类似
基类一般以Base前缀开头

### 2.3.3 内置基础类

1.  **BasePathHelperpath** 相关基础api的封装
    a. 类描述: 这个类用来封装一些常用的路径相关的方法
    b. 方法:
        i. `getNextFileName` 获取可用的文件名(如果已存在则"自增", "自增"指加特定规则的文件名后缀) 
        ii. path相关的方法的封装
            1. 处理不同系统下路径分隔符不一致的问题
            2. 处理开发环境和生产环境下的路径分隔符也不一致的问题
            3. 任何处理路径都需要使用这里的方法, 没有就加, 从而避免代码里面硬编码路径分隔符, 比如 `basePath + "//" + 'aaa'`
        iii. 一些常用路径的获取方法的封装
        iv. ...
    c. 属性:
        i. `fileHelper:T extends BaseFileHelper`
    d. 其它说明: 
        i. 这个基类需要针对其方法逐一做单元测试
2. **BaseFileHelper** file相关基础api的封装
    a. 类描述: 这个类用来封装一些常用的文件相关的方法
    b. 方法:
        i. `read`
            1. 不同环境下的文件读取兼容, 通过node的file api读取文件或者通过http接口读取
            2. 大文件的读取处理
        ii. `write`
        iii. `copy`
        iv. `rename`
        v. `save`
    c. 属性:
        i. `pathHelper:T extends BasePathHelper`
    d. 其它说明: 
        i. 这个基类需要针对其方法逐一做单元测试
3. **BaseDownlodHelper** 文件上传相关基础api的封装
    a. 类描述: 
        i. 文件上传相关的基础处理
        ii. 隔离出不同平台不同环境下的下载方案的差异, 主要是electron环境下与web环境的差异
    b. 方法: 
        i. `download`
            1. 不同环境下的文件下载兼容
    c. 属性:
        i. `fileHelper: T extends BaseFileHelper`
        ii. `pathHelper:T extends BasePathHelper`
4. **BaseUploadHelper** 文件下载相关基础api的封装
    a. 类描述: 
        i. 文件上传相关的基础处理
        ii. 隔离出不同平台不同环境下的上传方案的差异
    b. 方法: 
        i. `upload`
            1. 不同环境下的文件上传兼容
    c. 属性:
        i. `fileHelper: T extends BaseFileHelper`
        ii. `pathHelper: T extends BasePathHelper`


## 2.4 设计模式

### 2.4.1 基本准则

面对对象
解耦合
实施严格的类型检查
保持代码模块化和可测试性
遵循单一职责原则设计组件
确保良好的错误处理和边界情况处理
测试用例,覆盖各种可能性

创建一个 index.test.ts 文件,导入所需的测试框架和被测试的代码。
使用测试框架提供的 API 来编写测试用例,例如 Mocha 的 describe 和 it 函数。
测试用例标题不要超过30个汉字,测试步骤不要多于7步,预期结果不要多于5个。
选用合适的测试框架,配置测试环境,编写测试用例,并整合到CI/CD流程中。

### 2.4.2 视图层业务层分离

1. 对于复杂的 **.vue** 文件, 需要尽量将其分拆成多个 **.vue** 文件
   - 需要按照组件化的原则进行拆分子组件, 每个子组件一个文件夹
2. 对于部分业务逻辑比较多的 **.vue** 文件, 需要将业务逻辑抽离出一个 **.ts**文件
   - 这个 **.ts**文件主要负责业务逻辑, 通常里面一个业务类, 命名规则 XxxBusiness
   - 这个 **.ts**文件中还可以包含一系列Interface的声明, 或者某些特殊的不通用的工具函数
3. XxxBusiness继承自业务基类**BaseManager**
   - @BaseManager 中封装有 单例模式, 未来还将集成各种**Service**的入口, 以及事件中心@EventHelper的访问
   - **BaseManager** 还可以封装有Store的访问, 以及api等的访问
   - 提供业务相关的方法与业务相关的数据, 及其数据处理, 数据获取, 数据存储
4. **.vue**文件
   - **vue**文件主要负责视图的渲染, 和ui强相关的逻辑和响应式变量都放到这个文件中 (相当于视图层)
   - 以 **.vue** 文件为主来组织文件夹的结构
   - 这个文件集成的代码部分包括 **template**, **css**, **ref**, **ui**相关的响应式变量以及函数与生命周期

### 2.4.3 使用事件解耦

1. **EventHelper**作为事件中心, 其它所有的.vue和.ts都能通过它来抛出事件以及监听事件, 来达到解耦目的, 具体用法见@event.ts

