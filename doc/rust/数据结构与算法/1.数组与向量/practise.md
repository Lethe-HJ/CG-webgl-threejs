# 数组与向量 - 练习题

## 说明
- 所有练习题都要求使用 Rust 实现
- 尽量考虑边界情况和错误处理
- 注意所有权和借用规则
- 可以添加必要的测试用例

---

## 基础题

### 练习 1：数组求和
编写一个函数 `sum_array`，接受一个整数数组的切片，返回所有元素的和。

```rust
fn sum_array(arr: &[i32]) -> i32 {
    // 你的代码
}

// 测试用例
// assert_eq!(sum_array(&[1, 2, 3, 4, 5]), 15);
// assert_eq!(sum_array(&[]), 0);
```

### 练习 2：查找最大值
编写一个函数 `find_max`，接受一个整数数组的切片，返回最大值的引用。如果数组为空，返回 `None`。

```rust
fn find_max(arr: &[i32]) -> Option<&i32> {
    // 你的代码
}

// 测试用例
// assert_eq!(find_max(&[1, 5, 3, 9, 2]), Some(&9));
// assert_eq!(find_max(&[]), None);
```

### 练习 3：数组反转
编写一个函数 `reverse_array`，接受一个可变整数数组的切片，原地反转数组。

```rust
fn reverse_array(arr: &mut [i32]) {
    // 你的代码
}

// 测试用例
// let mut arr = [1, 2, 3, 4, 5];
// reverse_array(&mut arr);
// assert_eq!(arr, [5, 4, 3, 2, 1]);
```

### 练习 4：查找元素
编写一个函数 `find_index`，接受一个数组切片和一个值，返回该值第一次出现的索引。如果不存在，返回 `None`。

```rust
fn find_index(arr: &[i32], target: i32) -> Option<usize> {
    // 你的代码
}

// 测试用例
// assert_eq!(find_index(&[1, 2, 3, 4, 5], 3), Some(2));
// assert_eq!(find_index(&[1, 2, 3, 4, 5], 6), None);
```

### 练习 5：移除重复元素
编写一个函数 `remove_duplicates`，接受一个整数向量，移除重复元素并保持原有顺序，返回新向量。

```rust
fn remove_duplicates(vec: Vec<i32>) -> Vec<i32> {
    // 你的代码
}

// 测试用例
// assert_eq!(remove_duplicates(vec![1, 2, 2, 3, 3, 3, 4]), vec![1, 2, 3, 4]);
```

---

## 中等题

### 练习 6：合并两个有序数组
编写一个函数 `merge_sorted`，接受两个已排序的整数数组切片，合并它们并返回一个新的已排序向量。

```rust
fn merge_sorted(arr1: &[i32], arr2: &[i32]) -> Vec<i32> {
    // 你的代码
}

// 测试用例
// assert_eq!(
//     merge_sorted(&[1, 3, 5], &[2, 4, 6]),
//     vec![1, 2, 3, 4, 5, 6]
// );
```

### 练习 7：数组旋转
编写一个函数 `rotate_array`，接受一个可变数组切片和一个整数 `k`，将数组向右旋转 `k` 个位置。

```rust
fn rotate_array(arr: &mut [i32], k: usize) {
    // 你的代码
    // 例如：[1, 2, 3, 4, 5] 旋转 2 个位置 -> [4, 5, 1, 2, 3]
}

// 测试用例
// let mut arr = [1, 2, 3, 4, 5];
// rotate_array(&mut arr, 2);
// assert_eq!(arr, [4, 5, 1, 2, 3]);
```

### 练习 8：移动零
编写一个函数 `move_zeros`，接受一个可变整数向量，将所有 0 移动到末尾，保持其他元素的相对顺序。

```rust
fn move_zeros(vec: &mut Vec<i32>) {
    // 你的代码
}

// 测试用例
// let mut vec = vec![0, 1, 0, 3, 12];
// move_zeros(&mut vec);
// assert_eq!(vec, vec![1, 3, 12, 0, 0]);
```

### 练习 9：两数之和
编写一个函数 `two_sum`，接受一个整数数组切片和一个目标值，返回两个数的索引，使得它们的和等于目标值。假设每个输入只有一个解，且不能使用同一个元素两次。

```rust
fn two_sum(nums: &[i32], target: i32) -> Option<(usize, usize)> {
    // 你的代码
}

// 测试用例
// assert_eq!(two_sum(&[2, 7, 11, 15], 9), Some((0, 1)));
// assert_eq!(two_sum(&[3, 2, 4], 6), Some((1, 2)));
```

### 练习 10：子数组最大和
编写一个函数 `max_subarray_sum`，接受一个整数数组切片，返回连续子数组的最大和（Kadane 算法）。

```rust
fn max_subarray_sum(arr: &[i32]) -> i32 {
    // 你的代码
}

// 测试用例
// assert_eq!(max_subarray_sum(&[-2, 1, -3, 4, -1, 2, 1, -5, 4]), 6);
// assert_eq!(max_subarray_sum(&[1]), 1);
```

---

## 进阶题

### 练习 11：实现简单的动态数组
实现一个简单的动态数组 `MyVec`，至少包含以下功能：
- `new()` - 创建新的动态数组
- `push(value)` - 在末尾添加元素
- `pop()` - 移除并返回最后一个元素
- `get(index)` - 获取指定索引的元素
- `len()` - 获取长度
- `capacity()` - 获取容量

```rust
struct MyVec<T> {
    // 你的实现
}

impl<T> MyVec<T> {
    fn new() -> Self {
        // 你的代码
    }
    
    fn push(&mut self, value: T) {
        // 你的代码
    }
    
    fn pop(&mut self) -> Option<T> {
        // 你的代码
    }
    
    fn get(&self, index: usize) -> Option<&T> {
        // 你的代码
    }
    
    fn len(&self) -> usize {
        // 你的代码
    }
    
    fn capacity(&self) -> usize {
        // 你的代码
    }
}
```

### 练习 12：数组的滑动窗口最大值
编写一个函数 `max_sliding_window`，接受一个整数数组和一个窗口大小 `k`，返回每个窗口的最大值。

```rust
fn max_sliding_window(nums: &[i32], k: usize) -> Vec<i32> {
    // 你的代码
    // 例如：nums = [1,3,-1,-3,5,3,6,7], k = 3
    // 输出：[3,3,5,5,6,7]
}

// 测试用例
// assert_eq!(
//     max_sliding_window(&[1,3,-1,-3,5,3,6,7], 3),
//     vec![3,3,5,5,6,7]
// );
```

### 练习 13：三数之和
编写一个函数 `three_sum`，接受一个整数向量，找到所有不重复的三元组，使得它们的和为零。

```rust
fn three_sum(nums: Vec<i32>) -> Vec<Vec<i32>> {
    // 你的代码
    // 返回的三元组应该是不重复的
}

// 测试用例
// let result = three_sum(vec![-1, 0, 1, 2, -1, -4]);
// 应该包含 [-1, -1, 2] 和 [-1, 0, 1]
```

### 练习 14：合并区间
编写一个函数 `merge_intervals`，接受一个区间向量（每个区间用 `(start, end)` 表示），合并所有重叠的区间。

```rust
fn merge_intervals(intervals: Vec<(i32, i32)>) -> Vec<(i32, i32)> {
    // 你的代码
}

// 测试用例
// assert_eq!(
//     merge_intervals(vec![(1,3), (2,6), (8,10), (15,18)]),
//     vec![(1,6), (8,10), (15,18)]
// );
```

### 练习 15：数组中的第 K 大元素
编写一个函数 `find_kth_largest`，接受一个整数向量和一个整数 `k`，返回数组中第 `k` 大的元素。

```rust
fn find_kth_largest(nums: Vec<i32>, k: usize) -> i32 {
    // 你的代码
    // 可以使用排序，或者尝试更高效的方法
}

// 测试用例
// assert_eq!(find_kth_largest(vec![3,2,1,5,6,4], 2), 5);
// assert_eq!(find_kth_largest(vec![3,2,3,1,2,4,5,5,6], 4), 4);
```

---

## 挑战题

### 练习 16：实现一个通用的栈（使用 Vec）
实现一个泛型栈结构，使用 `Vec` 作为底层存储。

```rust
struct Stack<T> {
    // 你的实现
}

impl<T> Stack<T> {
    fn new() -> Self {
        // 你的代码
    }
    
    fn push(&mut self, value: T) {
        // 你的代码
    }
    
    fn pop(&mut self) -> Option<T> {
        // 你的代码
    }
    
    fn peek(&self) -> Option<&T> {
        // 你的代码
    }
    
    fn is_empty(&self) -> bool {
        // 你的代码
    }
    
    fn len(&self) -> usize {
        // 你的代码
    }
}
```

### 练习 17：螺旋矩阵
编写一个函数 `spiral_order`，接受一个二维向量（矩阵），按螺旋顺序返回所有元素。

```rust
fn spiral_order(matrix: Vec<Vec<i32>>) -> Vec<i32> {
    // 你的代码
}

// 测试用例
// let matrix = vec![
//     vec![1, 2, 3],
//     vec![4, 5, 6],
//     vec![7, 8, 9]
// ];
// assert_eq!(spiral_order(matrix), vec![1, 2, 3, 6, 9, 8, 7, 4, 5]);
```

### 练习 18：接雨水
编写一个函数 `trap_rain_water`，接受一个表示高度的整数数组，计算能够接多少雨水。

```rust
fn trap_rain_water(height: Vec<i32>) -> i32 {
    // 你的代码
}

// 测试用例
// assert_eq!(trap_rain_water(vec![0,1,0,2,1,0,1,3,2,1,2,1]), 6);
// assert_eq!(trap_rain_water(vec![4,2,0,3,2,5]), 9);
```

---

## 提示

1. **练习 1-5**：基础操作，熟悉数组和向量的基本方法
2. **练习 6-10**：需要一些算法思维，注意边界情况
3. **练习 11**：理解动态数组的内部实现，注意所有权和内存管理
4. **练习 12-15**：需要更复杂的算法，可能需要排序或使用额外的数据结构
5. **练习 16-18**：综合应用，结合多个概念

## 测试建议

为每个函数编写测试用例，例如：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    
    #[test]
    fn test_sum_array() {
        assert_eq!(sum_array(&[1, 2, 3, 4, 5]), 15);
        assert_eq!(sum_array(&[]), 0);
        assert_eq!(sum_array(&[10]), 10);
    }
}
```

Good luck! 🚀

